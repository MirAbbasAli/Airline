spring:
 application:
  name: PaymentMS
 cloud:
  consul:
   host: localhost
   config:
    fail-fast: false
    enabled: true
    prefixes: config
    defaultContext: application
    profileSerparator: '::'
    format: YAML