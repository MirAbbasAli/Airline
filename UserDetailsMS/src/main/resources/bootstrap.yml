spring:
 application:
  name: UserDetailsMS
 cloud:
  consul:
   host: localhost
   config:
    fail-fast: false
    enabled: true
    prefixes: config
    defaultContext: application
    profileSerparator: '::'
    format: YAML